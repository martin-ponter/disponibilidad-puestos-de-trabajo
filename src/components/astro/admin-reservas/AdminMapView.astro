<div id="admin-map-view" class="rounded-3xl border border-slate-200 bg-white p-5 shadow-xl shadow-slate-200/70 sm:p-6">
  <div class="mb-5 flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between">
    <div>
      <h2 class="text-lg font-semibold text-slate-900">Mapa de sala</h2>
      <p id="admin-map-subtitle" class="mt-1 text-sm text-slate-500">
        Selecciona una oficina y una sala para visualizar el plano.
      </p>
    </div>

    <div class="rounded-2xl border border-slate-200 bg-slate-50 px-4 py-2 text-sm text-slate-600">
      Haz clic en una mesa ocupada o en un empleado de la lista para editar.
    </div>
  </div>

  <div class="mb-4 rounded-2xl border border-slate-200 bg-slate-50 p-4">
    <div class="grid gap-3 text-sm text-slate-600 sm:grid-cols-2 xl:grid-cols-4">
      <div class="flex items-center gap-2">
        <span class="h-3 w-3 rounded-full bg-emerald-400"></span>
        Libre
      </div>
      <div class="flex items-center gap-2">
        <span class="h-3 w-3 rounded-full bg-sky-400"></span>
        Ocupada
      </div>
      <div class="flex items-center gap-2">
        <span class="h-3 w-3 rounded-full bg-blue-500"></span>
        Seleccionada
      </div>
      <div class="flex items-center gap-2">
        <span class="h-3 w-3 rounded-full bg-rose-400"></span>
        No disponible
      </div>
    </div>
  </div>

  <div
    id="admin-map-empty-state"
    class="flex min-h-[420px] items-center justify-center rounded-[28px] border border-dashed border-slate-300 bg-slate-50 p-8 text-center"
  >
    <div class="max-w-md">
      <div class="mx-auto mb-4 flex h-16 w-16 items-center justify-center rounded-2xl bg-white text-2xl shadow-sm">
        🗺️
      </div>
      <h3 class="text-lg font-semibold text-slate-900">Aún no hay plano cargado</h3>
      <p class="mt-2 text-sm leading-6 text-slate-500">
        Selecciona oficina y sala para ver el mapa y las asignaciones del día.
      </p>
    </div>
  </div>

  <div id="admin-map-wrapper" class="hidden">
    <div class="overflow-hidden rounded-[28px] border border-slate-200 bg-slate-50 p-4 sm:p-6">
      <div class="mb-4 flex flex-wrap items-center justify-between gap-3">
        <div class="rounded-2xl bg-white px-4 py-2 text-sm text-slate-600 shadow-sm">
          Plano de asignaciones
        </div>

        <button
          id="assign-first-free-btn"
          type="button"
          class="inline-flex items-center justify-center rounded-2xl border border-slate-200 bg-white px-4 py-2 text-sm font-medium text-slate-700 transition hover:bg-slate-100 disabled:cursor-not-allowed disabled:opacity-50"
          disabled
        >
          Asignar primera libre
        </button>
      </div>

      <div
        id="admin-office-map"
        class="relative min-h-[420px] rounded-[24px] border border-slate-200 bg-gradient-to-br from-slate-100 to-white p-4 sm:min-h-[520px] sm:p-6"
      >
        <div class="absolute left-4 top-4 rounded-xl border border-slate-200 bg-white px-3 py-2 text-xs text-slate-600 sm:left-6 sm:top-6">
          Entrada
        </div>

        <div class="absolute right-4 top-4 rounded-xl border border-slate-200 bg-white px-3 py-2 text-xs text-slate-600 sm:right-6 sm:top-6">
          Ventanas
        </div>

        <div class="absolute left-1/2 top-6 h-14 w-32 -translate-x-1/2 rounded-2xl border border-slate-200 bg-slate-200/70"></div>
        <div class="absolute bottom-6 left-6 right-6 h-3 rounded-full bg-slate-200"></div>

        <div
          id="admin-desks-grid"
          class="grid min-h-[360px] grid-cols-2 gap-4 pt-20 sm:grid-cols-3 lg:grid-cols-4"
        ></div>
      </div>
    </div>
  </div>
</div>