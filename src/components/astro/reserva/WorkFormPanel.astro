<aside class="rounded-3xl border border-slate-200 bg-slate-50 p-5">
  <div class="mb-5">
    <h4 class="text-lg font-semibold text-slate-900">Datos de la jornada</h4>
    <p class="mt-1 text-sm text-slate-500">
      Completa la información para cargar el plano y gestionar tu reserva.
    </p>
  </div>

  <form id="reservation-form" class="space-y-5">
    <div>
      <label for="office" class="mb-2 block text-sm font-medium text-slate-700">
        Oficina
      </label>
      <select
        id="office"
        name="office"
        class="w-full rounded-2xl border border-slate-200 bg-white px-4 py-3 text-sm text-slate-900 outline-none transition focus:border-blue-400 focus:ring-2 focus:ring-blue-500/20"
      >
        <option value="">Selecciona una oficina</option>
        <option value="Toledo">Toledo</option>
        <option value="Madrid">Madrid</option>
        <option value="Alcobendas">Alcobendas</option>
        <option value="Consuegra">Consuegra</option>
      </select>
    </div>

    <div>
      <label for="room" class="mb-2 block text-sm font-medium text-slate-700">
        Sala
      </label>
      <select
        id="room"
        name="room"
        disabled
        class="w-full rounded-2xl border border-slate-200 bg-white px-4 py-3 text-sm text-slate-900 outline-none transition disabled:cursor-not-allowed disabled:bg-slate-100 disabled:opacity-70 focus:border-blue-400 focus:ring-2 focus:ring-blue-500/20"
      >
        <option value="">Primero elige una oficina</option>
      </select>
    </div>

    <div class="grid gap-4 sm:grid-cols-2">
      <div>
        <label for="start-time" class="mb-2 block text-sm font-medium text-slate-700">
          Hora de inicio
        </label>
        <input
          id="start-time"
          name="start-time"
          type="time"
          class="w-full rounded-2xl border border-slate-200 bg-white px-4 py-3 text-sm text-slate-900 outline-none transition focus:border-blue-400 focus:ring-2 focus:ring-blue-500/20"
        />
      </div>

      <div>
        <label for="end-time" class="mb-2 block text-sm font-medium text-slate-700">
          Hora de fin
        </label>
        <input
          id="end-time"
          name="end-time"
          type="time"
          class="w-full rounded-2xl border border-slate-200 bg-white px-4 py-3 text-sm text-slate-900 outline-none transition focus:border-blue-400 focus:ring-2 focus:ring-blue-500/20"
        />
      </div>
    </div>

    <div class="rounded-2xl border border-slate-200 bg-white p-4">
      <p class="mb-3 text-sm font-medium text-slate-700">Tipo de jornada</p>

      <label class="flex items-start gap-3 rounded-2xl border border-slate-200 p-3 transition hover:bg-slate-50">
        <input
          id="remote-checkbox"
          type="checkbox"
          class="mt-1 h-4 w-4 rounded border-slate-300 text-blue-600 focus:ring-blue-500"
        />
        <div>
          <span class="block text-sm font-medium text-slate-900">Teletrabajo</span>
          <span class="mt-1 block text-sm text-slate-500">
            No necesito reservar una mesa en oficina.
          </span>
        </div>
      </label>

      <label class="mt-3 flex items-start gap-3 rounded-2xl border border-slate-200 p-3 transition hover:bg-slate-50">
        <input
          id="event-checkbox"
          type="checkbox"
          class="mt-1 h-4 w-4 rounded border-slate-300 text-blue-600 focus:ring-blue-500"
        />
        <div>
          <span class="block text-sm font-medium text-slate-900">Evento</span>
          <span class="mt-1 block text-sm text-slate-500">
            Estaré fuera de mi puesto habitual y no necesito mesa.
          </span>
        </div>
      </label>
    </div>

    <div class="rounded-2xl border border-slate-200 bg-white p-4">
      <div class="mb-3 flex items-center justify-between">
        <span class="text-sm font-medium text-slate-700">Estado de las mesas</span>
      </div>

      <div class="grid grid-cols-1 gap-3 text-sm text-slate-600 sm:grid-cols-3">
        <div class="flex items-center gap-2">
          <span class="h-3 w-3 rounded-full bg-emerald-400"></span>
          Disponible
        </div>
        <div class="flex items-center gap-2">
          <span class="h-3 w-3 rounded-full bg-rose-400"></span>
          No disponible
        </div>
        <div class="flex items-center gap-2">
          <span class="h-3 w-3 rounded-full bg-blue-400"></span>
          Seleccionada
        </div>
      </div>
    </div>
  </form>
</aside>